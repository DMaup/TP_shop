class Seller {
    constructor(seller_name, seller_firstname) {

        /*  this.structure = structure;

          this.g_marker = null;
          this.g_infowindows = null;

          this.createG_marker(map, position, shop);
          this.createG_infowindow(shop, description);
          this.linkMarkerWindow(map);
  */


    }
}